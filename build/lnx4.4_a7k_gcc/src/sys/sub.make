###############################################################################
#########################  lib-of LIBRARY   ###################################
###############################################################################
OBJ := sys_dma.o
INC := $(TOPINC)
DEF := $(TOPDEF)

lsys.o: $(OBJ)
	@(echo "(LD)  $@ <= $^")
	@($(LD) -r -o $@ -Map $*.map $^)

include $(TOPDIR)/Rules.make
