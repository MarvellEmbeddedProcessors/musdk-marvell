###############################################################################
###########################  lib LIBRARY   ####################################
###############################################################################
SUBDIRS := of

OBJ := list.o
INC := $(TOPINC)
DEF := $(TOPDEF)

llib.o: $(OBJ) llibof.o
	@(echo "(LD)  $@ <= $^")
	@($(LD) -r -o $@ -Map $*.map $^)

include $(TOPDIR)/Rules.make
