###############################################################################
###########################  lib LIBRARY   ####################################
###############################################################################
SUBDIRS := uio

OBJ := list.o lib_misc.o mem_mng.o
INC := $(TOPINC)
DEF := $(TOPDEF)

llib.o: $(OBJ) llibuio.o
	@(echo "(LD)  $@ <= $^")
	@($(LD) -r -o $@ -Map $*.map $^)

include $(TOPDIR)/Rules.make
