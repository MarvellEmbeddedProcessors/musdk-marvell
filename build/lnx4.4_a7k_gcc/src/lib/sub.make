###############################################################################
###########################  lib LIBRARY   ####################################
###############################################################################
SUBDIRS := of uio

OBJ := list.o misc.o
INC := $(TOPINC)
DEF := $(TOPDEF)

llib.o: $(OBJ) llibof.o llibuio.o
	@(echo "(LD)  $@ <= $^")
	@($(LD) -r -o $@ -Map $*.map $^)

include $(TOPDIR)/Rules.make
