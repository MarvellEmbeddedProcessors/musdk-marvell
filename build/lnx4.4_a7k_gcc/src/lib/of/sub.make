###############################################################################
#########################  lib-of LIBRARY   ###################################
###############################################################################
OBJ := of.o
INC := $(TOPINC)
DEF := $(TOPDEF)

llibof.o: $(OBJ)
	@(echo "(LD)  $@ <= $^")
	@($(LD) -r -o $@ -Map $*.map $^)

include $(TOPDIR)/Rules.make
