###############################################################################
#############################  drivers sub.make   #############################
###############################################################################
OBJ := hif.o bpool.o ppio.o
INC := $(TOPINC)
DEF := $(TOPDEF)

lpp2.o: $(OBJ)
	@(echo "(LD)  $@ <= $^")
	@($(LD) -r -o $@ -Map $*.map $^)

include $(TOPDIR)/Rules.make
