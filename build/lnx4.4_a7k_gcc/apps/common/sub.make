###############################################################################
###########################  apps common sub.make   ###########################
###############################################################################
SUBDIRS := lib

OBJ := mvapp.o
INC := $(TOPINC)
DEF := $(TOPDEF)

lcommon.o: lcommon_lib.o $(OBJ)
	@(echo "(LD)  $@ <= $^")
	@($(LD) -r -o $@ -Map $*.map $^)

include $(TOPDIR)/Rules.make
