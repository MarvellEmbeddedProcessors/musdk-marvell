###############################################################################
###########################  apps common sub.make   ###########################
###############################################################################
SUBDIRS := lib

INC := $(TOPINC)
DEF := $(TOPDEF)

lcommon.o: lcommon_lib.o
	@(echo "(LD)  $@ <= $^")
	@($(LD) -r -o $@ -Map $*.map $^)

include $(TOPDIR)/Rules.make
